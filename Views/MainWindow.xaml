<Window x:Class="FishNovel.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FishNovel"
        mc:Ignorable="d"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        WindowStartupLocation="CenterScreen"
        Title="MainWindow" Height="600" Width="900">
    <Grid Name="MainGrid">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="25"/>
            </Grid.RowDefinitions>
            <DockPanel Grid.Row="0" Background="#FFEDEDED">
                <TextBlock Name="AD" Text="广告位招租" FontSize="16" FontWeight="Bold" VerticalAlignment="Center" Margin="10,0,0,0"/>
            </DockPanel>
            
            <Grid Grid.Row="1" Name="ContentGrid" Background="AliceBlue">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="400"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid Name="WorkGrid" >
                    
                    
                </Grid>
                <Grid Name="PreviewGrid" Grid.Column="1">
                    <wv2:WebView2 Name="Preview"/>
                </Grid>
            </Grid>

            <DockPanel Grid.Row="2" Background="#FFEDEDED">
                <TextBlock Name="Ver" Text="v1.0.0 —— “规则就是用来打破的”" Foreground="Gray" VerticalAlignment="Center" Margin="10,0,0,0"/>
                <TextBlock Name="Copy" Text="Copyright © 2026 yl_lty. All rights reserved." Foreground="Gray" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="10,0,10,0"/>
            </DockPanel>
        </Grid>
        <StackPanel Grid.Row="0" Name="Toast" HorizontalAlignment="Center">
            <!--<Border Background="#FFE6B0B0" CornerRadius="5" Padding="8" Margin="8">
    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
        <materialDesign:PackIcon Kind="InfoOutline" Width="16" Height="26" Foreground="AliceBlue"/>
        <TextBlock Name="ToastText" Text="123" Foreground="AliceBlue" VerticalAlignment="Center"/>
    </StackPanel>
</Border>-->
        </StackPanel>
    </Grid>
</Window>
